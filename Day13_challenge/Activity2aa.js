// import file
const {square,mul,my_object}=require('./Activity2a')

const product=mul(7,3)
console.log(`The product of the numbers is ${product}`);

const squ=square(7)
console.log(`The square of the number is ${squ}`);
const name=my_object.name
console.log(`Welcome back ${name}`);